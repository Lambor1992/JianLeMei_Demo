<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="3dp">

    <ImageView
        android:id="@+id/notification_imageView"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_margin="3dp"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/fileName"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignBottom="@id/notification_imageView"
        android:layout_toRightOf="@id/notification_imageView"
        android:gravity="center_vertical"
        android:textColor="@android:color/white" />

    <TextView
        android:id="@+id/notification_rate"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignRight="@id/notification_imageView"
        android:layout_below="@id/notification_imageView"
        android:gravity="center"
        android:text="0%"
        android:textColor="@android:color/white" />

    <ProgressBar
        android:id="@+id/notification_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/fileName"
        android:layout_below="@id/fileName"
        android:max="100"
        android:progress="0" />

</RelativeLayout>